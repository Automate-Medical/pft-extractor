{
  "name": "pftx",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "sst test",
    "start": "sst start",
    "build": "sst build",
    "deploy": "sst deploy --outputs-file outputs.cdk.json",
    "remove": "sst remove"
  },
  "devDependencies": {
    "@aws-cdk/assert": "1.105.0",
    "@types/aws-lambda": "^8.10.70",
    "@types/jest": "^26.0.22",
    "@types/uuid": "^8.3.0",
    "jest": "^26.0.0"
  },
  "dependencies": {
    "@aws-cdk/core": "1.105.0",
    "@serverless-stack/cli": "0.26.1",
    "@serverless-stack/resources": "0.26.1",
    "uuid": "^3.3.2"
  }
}
